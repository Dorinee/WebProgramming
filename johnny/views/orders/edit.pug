extends ../layout

block content
  .container
    h1.main-title reservation modification
    form(action=`/orders/${order.id}`, method="POST")
      input(type="hidden", name="_method", value="PUT")
      include _form
      button.btn.btn-outline-primary(type="submit") modify
